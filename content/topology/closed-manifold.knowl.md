+++
id = "topology/closed-manifold"
title = "Closed manifold"
kind = "knowl"
summary = "A compact manifold without boundary."
aliases = ["closed manifold", "closed smooth manifold", "closed topological manifold"]
domains = ["topology", "fiber-bundles"]
prerequisites = ["topology/topological-manifold", "topology/compact-set"]
dependency_heuristic = "axiomatic-dependency-review-v1"
dependency_review_count = 2
+++

A **closed manifold** is a [[topology/topological-manifold|topological manifold]] that is [[topology/compact-set|compact]] and has no boundary.

## Smooth manifolds

A closed smooth manifold is a [[fiber-bundles/smooth-manifold|smooth manifold]] whose underlying topological manifold is closed.

Here “closed” does not merely mean closed as a subset of some ambient Euclidean space; it is intrinsic shorthand for compact without boundary. Spheres and tori are closed, while positive-dimensional Euclidean space and a positive-dimensional closed disk are not—the former is noncompact and the latter has boundary.
