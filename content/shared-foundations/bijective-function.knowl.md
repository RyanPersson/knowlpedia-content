+++
id = "shared-foundations/bijective-function"
title = "Bijective function"
kind = "knowl"
summary = "A function that is both one-to-one and onto"
aliases = ["bijective-function", "Bijective function"]
domains = ["shared-foundations"]
legacy_source_path = "shared-foundations/bijective-function.md"
+++

A **bijective function** is a [[shared-foundations/function|function]] $f:A\to B$ that is both [[shared-foundations/injective-function|injective]] and [[shared-foundations/surjective-function|surjective]].

A bijection sets up a perfect pairing between elements of the domain and codomain, and it is exactly the situation in which an [[shared-foundations/inverse-function|inverse function]] exists. Two sets have the same [[shared-foundations/cardinality|cardinality]] precisely when there is a bijection between them.

**Examples:**
- The function $f:\mathbb{Z}\to\mathbb{Z}$ given by $f(n)=n+1$ is bijective.
- If $A=\{1,2,3\}$ and $B=\{a,b,c\}$, the function defined by $1\mapsto a$, $2\mapsto b$, $3\mapsto c$ is bijective.
