+++
id = "real-analysis/bounded-above"
title = "Bounded above"
kind = "knowl"
summary = "A set of real numbers that has an upper bound."
aliases = ["bounded-above", "Bounded above"]
domains = ["real-analysis"]
legacy_source_path = "real-analysis/bounded-above.md"
+++

A **bounded above** set is a [[shared-foundations/subset|subset]] $A\subseteq\mathbb R$ for which there exists $M\in\mathbb R$ such that $x\le M$ for every $x\in A$; such an $M$ is called an upper bound of $A$.

This notion depends on the [[real-analysis/order-axioms|order axioms]] and is the basic hypothesis needed to define the [[real-analysis/supremum|supremum]]. A set can be bounded above without having a [[real-analysis/maximum|maximum]].

**Examples:**
- $A=(0,1)$ is bounded above (for example, by $M=1$).
- $A=\mathbb R$ is not bounded above.
