+++
id = "real-analysis/partition-of-an-interval"
title = "Partition of an interval"
kind = "knowl"
summary = "A finite increasing sequence of points that subdivides a closed interval."
aliases = ["partition-of-an-interval", "Partition of an interval"]
domains = ["real-analysis"]
legacy_source_path = "real-analysis/partition-of-an-interval.md"
+++

A **partition of an interval** $[a,b]$ is a finite sequence of real numbers
\[
P=\{x_0,x_1,\dots,x_n\}\quad\text{with}\quad a=x_0<x_1<\cdots<x_n=b.
\]
The associated subintervals are $[x_{i-1},x_i]$ for $i=1,\dots,n$.

Partitions of the [[real-analysis/interval|interval]] $[a,b]$ are used to build [[real-analysis/riemann-sum|Riemann sums]] and the [[real-analysis/upper-sum|upper]]/[[real-analysis/lower-sum|lower sums]] that define the [[real-analysis/riemann-integral|Riemann integral]].

**Examples:**
- The uniform partition of $[0,1]$ into $n$ pieces is $P=\{0,\tfrac1n,\tfrac2n,\dots,1\}$.
- A non-uniform partition of $[0,3]$ is $P=\{0,\tfrac12,1,3\}$.
