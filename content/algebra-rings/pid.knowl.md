+++
id = "algebra-rings/pid"
title = "Principal ideal domain"
kind = "knowl"
summary = "An integral domain in which every ideal is generated by a single element."
aliases = ["pid", "Principal ideal domain"]
domains = ["algebra-rings"]
legacy_source_path = "algebra-rings/pid.md"
+++

A **principal ideal domain (PID)** is an [[algebra-rings/integral-domain|integral domain]] $R$ such that every [[algebra-rings/ideal|ideal]] $I\subseteq R$ is [[algebra-rings/principal-ideal|principal]], i.e. $I=(a)$ for some $a\in R$.

PIDs provide strong control of divisibility and modules, and they sit between Euclidean domains and unique factorization: every [[algebra-rings/euclidean-domain|Euclidean domain]] is a PID, and every PID is a [[algebra-rings/ufd|UFD]] (see [[algebra-rings/pid-implies-ufd|PID implies UFD]]).

**Examples:**
- $\mathbb{Z}$ is a PID.
- If $k$ is a field, then $k[x]$ is a PID (indeed Euclidean).
- $k[x,y]$ is not a PID since $(x,y)$ is not principal.
